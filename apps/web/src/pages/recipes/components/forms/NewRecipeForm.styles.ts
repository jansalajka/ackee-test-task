import { colors } from '@workspace/ui';
import { typographyStyles } from '~modules/fela/styles/typography';

export const formStyles = {
    padding: '16px',
};

export const firstSectionHeaderStyles = {
    ...typographyStyles.formSectionHeader,
    marginTop: '16px',
    marginBottom: '16px',
};

export const errorMessageStyles = {
    ...typographyStyles.errorMessage,
    marginTop: '4px',
};

